{
  "name": "attendance-manager-server",
  "version": "1.0.0",
  "description": "Backend Proxy for ARMS Sync — deployed on Render.com",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --ignore *.json server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.12",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "node-cron": "^4.2.1",
    "nodemailer": "^8.0.10",
    "puppeteer": "^21.0.0"
  }
}
